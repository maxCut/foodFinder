import React from 'react'
import { Button } from '@mui/material'

const AddToCartButton = () => {
  return (
    <Button elevation={0} variant='contained' sx={{ width: '100%' }}>
      Add to Cart
    </Button>
  )
}

export default AddToCartButton
